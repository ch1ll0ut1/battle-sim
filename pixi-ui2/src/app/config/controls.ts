/**
 * Centralized control configuration for camera input
 * Defines key mappings and mouse settings for camera controls
 */
export const controls = {
    /**
     * Keys for moving camera up
     */
    up: ['w', 'arrowup'],

    /**
     * Keys for moving camera down
     */
    down: ['s', 'arrowdown'],

    /**
     * Keys for moving camera left
     */
    left: ['a', 'arrowleft'],

    /**
     * Keys for moving camera right
     */
    right: ['d', 'arrowright'],

    /**
     * Mouse button for dragging (0=left, 1=middle, 2=right)
     */
    dragButton: 2,

    /**
     * Mouse wheel zoom sensitivity (lower = less sensitive)
     */
    zoomSensitivity: 0.2,
};
