/**
 * Enum defining all available screens in the application
 */
export enum ScreenType {
    mainMenu = 'main_menu',
    mapEditor = 'map_editor',
    mapSelection = 'map_selection',
    game = 'game',
}
